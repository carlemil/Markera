# Plan: series backend + login + caliber

Requested 2026-09-06. Source of truth for the orchestrated work; one task = one commit.

## Goal

Every scanned series (the detected holes with their scores) is saved automatically to a
backend running in Docker on the Mac mini (`ssh macmini`, 192.168.1.191), tagged with a
timestamp and a caliber. Users log in with Google (Android) or Apple (iOS). The caliber is
shown next to the scanned image; the default "no caliber" (-) triggers a chooser dialog
after holes are detected.

Calibers (task 105): `22lr, 22wmr, 17hmr, 32, 380, 9mm, 38, 357, 40, 10mm, 44, 45, 223, 243, 6.5x55, 6.5cm, 270, 308, 30-06, 7.62x39, 8x57, 9.3x62, 300wm` plus `-` (none, default). The same list lives in the app's `Caliber` enum and the server's `CALIBERS`; a label the server does not know is a 400 on save, and labels are stored rows, so add and reorder freely but never rename.

## Decisions (pinned)

- **Backend = standalone Gradle project in `server/`** (Ktor, SQLite via `sqlite-jdbc`,
  `java-jwt` + `jwks-rsa` for Google/Apple ID-token verification). Not included in the
  root Gradle build: the root build needs the Android SDK at configuration time, which
  would break a `docker build`. Its two DTOs are duplicated in the app rather than shared.
- **Auth model**: the app exchanges a provider ID token once (`POST /auth/google`,
  `POST /auth/apple`) for an opaque backend session token stored in `sessions`;
  every later call sends `Authorization: Bearer <token>`. Google ID tokens live 1 h and
  Apple's 10 min, so re-sending them per request is not viable.
- **Dev auth** (`POST /auth/dev`, only when `DEV_AUTH=true`) lets the mock flavor and
  tests log in without Google Cloud / Apple Developer configuration.
- **Runtime on the Mac mini = Colima + docker CLI via Homebrew** (headless, no Docker
  Desktop GUI login). `brew services start colima` for autostart; `docker compose up -d`
  from a clone at `~/source/Markera`. SQLite file on a named volume.
- **Backend URL** is a Gradle property `markera.backend.url`, default
  `http://192.168.1.191:8090` (8080 on the Mac mini is taken by an nginx site); cleartext allowed for that host only via a network
  security config.
- **Auto-save hook lives in `TargetScanController`** (shared by free marking and the
  competition wizard): after `onHolesDetected` with ≥1 hole it emits a pending series.
  A nav-host-level saver reads the persisted caliber, shows the chooser dialog when it
  is `-`, then posts. Not logged in → no save, a "sign in to save" hint instead.
- **Caliber badge** is drawn inside the shared `TargetScanner` viewport (top-right, frozen
  frame only); tapping it opens the chooser. One component serves both screens.
- Images: task 8 adds `POST /series/{id}/image` as a raw `image/jpeg` body (no multipart), stored
  as `/data/images/<id>.jpg` on the volume; upload happens after the series POST succeeds and
  its failure never fails the series. The recorder stays commonMain: JPEG encoding is injected
  (`encodeJpeg: suspend (PlatformImage) -> ByteArray`) like the caliber persistence.
- **Admin UI** (task 10, requested 2026-09-06): lives in the Ktor server, plain server-rendered
  HTML from string templates (no template/HTML dependency), read-only, no auth. Gated by
  `ADMIN_UI=true` so a future public deployment cannot expose it by accident.
- **Save timing** (task 11, requested 2026-09-06): a scan is only *pending* while its frozen
  frame is on screen; the POST (and the caliber dialog when the caliber is `-`) happens when the
  user returns to the camera.
- **Edited scores** (task 15, requested 2026-09-06, for training data later): the score
  pickers are *positional* — `topScores[i]` is built from `scores[i]` (`MarkeraViewModelImpl.
  onHolesDetected`, first `SCORE_PICKER_COUNT` = 5 holes, padded with 0) and edited in place —
  so picker slot `i` maps back onto detected hole `i`. Each saved hole carries both the value
  the user confirmed (`ring`, `innerTen`, the existing fields — history totals and the admin
  pages keep using them) and what the detector said (`detectedRing`, `detectedInnerTen`,
  `null` when the detector said nothing). Three kinds of hole, all derived, nothing stored
  for the kind itself: **detected** (`detectedRing != null`; `edited` = the confirmed pair
  differs from the detected pair), **manual** (task 18: placed by a tap on the frozen photo,
  so it has `x`/`y`/`distanceMm` scored by the same ellipse geometry, but `detected*` null),
  **typed** (a score entered in an empty picker slot: `x`/`y`/`distanceMm` null — those become
  nullable — and `detected*` null). A detected hole the user sets to 0 is kept as a 0 (a
  false-positive signal). Detected holes beyond the 5 picker slots are saved unedited.
  Merging happens in the app (`SeriesRecorder.commit(topScores)`); the server only stores.
  Ordering: server half first and deployed (the server JSON is strict about unknown keys),
  then the app half. `HitScore` gains `manual: Boolean = false` so the DTO mapping and the
  overlay colour know the kind.
- **Production hardening** (requested 2026-09-07, tasks 24–30): the admin UI is gated by a
  password (`ADMIN_PASSWORD`, HTTP Basic, user `admin`; unset = admin routes off — replaces the
  `ADMIN_UI` flag) and `DEV_AUTH=false` on the Mac mini, so the mock flavor can no longer sign in
  against it (point it at a local server with `DEV_AUTH=true`, or re-enable temporarily).
  Pagination is cursor-based: `GET /series?limit=N&before=<id>` (newest first, default 50, max
  200; response stays a plain array). `DELETE /series/{id}` (owner only; holes + image go too)
  and `DELETE /account` (everything the user owns, sessions included; the app signs out after).
  Manual markers are removed by tapping them again (within the same 24 dp gap); a removed hole
  shifts the picker slots after it left by one, so edits stay aligned. Release signing: upload
  keystore at the repo root (`keystore`, gitignored) read from `keystore.properties`; Play App
  Signing re-signs, so the Play-generated key's SHA-1 is what goes into the Android OAuth client.
- Sub-agents run on `opus`.

## User actions needed (cannot be done by the orchestrator)

- [x] Google Cloud Console (done 2026-09-06; Web client id in local.properties + Mac .env, Android client with debug SHA-1): an OAuth **Web** client ID (used as the token audience on the
      backend and as `serverClientId` in the app) and an **Android** client ID with the
      debug/release SHA-1 for `se.kjellstrand.markera`. Put the Web client ID in
      `local.properties` as `markera.google.client.id=...` and in the Mac mini's
      `server/.env` as `GOOGLE_CLIENT_ID=...`.
- [x] Apple Developer: "Sign in with Apple" on the iOS bundle id (the fastlane lane enabled it 2026-09-10);
      `APPLE_BUNDLE_ID` set in the Mac's `server/.env`, container rebuilt 2026-09-10.
- [ ] Apple Developer, for Sign in with Apple **on Android** (task 119): create a **Services ID**
      (e.g. `se.kjellstrand.markera.web`) whose *Primary App ID* is `se.kjellstrand.markera` — that is
      what makes Apple return the same `sub` to Android as to the iPhone; anything else lands the user
      on a second, empty account. Domain `markera.duckdns.org`, Return URL exactly
      `https://markera.duckdns.org/auth/apple/callback`. Drop the domain-association file Apple offers at
      `server/src/main/resources/apple-developer-domain-association.txt` (the route serving it exists) and
      press **Verify**. Then Keys → + → Sign in with Apple, same Primary App ID; the `.p8` downloads once.
      Finally paste into the Mac's `server/.env`: `APPLE_SERVICES_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID`,
      `APPLE_PRIVATE_KEY` (the `.p8` flattened: `grep -v -- '-----' AuthKey_*.p8 | tr -d '
'`) and
      `PUBLIC_URL=https://markera.duckdns.org`, and say the word — I redeploy. `APPLE_BUNDLE_ID` stays.
- [x] App Store Connect: app record + API key in `iosApp/fastlane/.env` on the Mac; build 1.5.0 (6)
      uploaded to TestFlight 2026-09-10 with `scripts/mac-beta.sh`.
- [ ] Play Console → Setup → API access: link a Cloud project, create a service account with the
      "Release manager" role, download its JSON key to `play-account.json` at the repo root
      (gitignored). Needed by `/release`.
- [x] (done 2026-09-08: Android OAuth client with the Play SHA-1 saved; Google sign-in + Historik verified on the Play-installed 1.2.0 (3)) After the first upload with Play App Signing: copy the **App signing key certificate**
      SHA-1 (Play Console → Setup → App signing) into the Android OAuth client in Google Cloud
      Console (or add a second Android client with it). Until then the release build's Google
      sign-in fails. The upload key's SHA-1 is printed by task 26 for reference.
      Read from the Play-installed 1.2.0 (3) on 2026-09-08: app signing key SHA-1
      `92:96:43:90:D0:7B:94:74:B7:BA:83:CE:7E:10:79:C4:E0:A0:A3:49` (CN=Android, O=Google Inc.).
- [x] Privacy policy: Play Console now points at `https://markera.duckdns.org/privacy` (swapped
      2026-09-08 via the browser; saved as a pending change — the user sends it for review from
      Publishing overview, best together with the Data safety fix). All 10 App content declarations
      are actioned.
- [ ] Data safety declaration is wrong (says "App doesn't collect or share data", "Data isn't
      encrypted"): the app collects account info (Google sign-in name + subject), photos (target
      frames) and user content (series), all encrypted in transit (HTTPS), with in-app deletion. Fix
      in Play Console → App content → Data safety → Manage; the account-deletion URL
      `https://markera.duckdns.org/delete-account` (task 61) goes into that form's data-deletion
      section.
- [x] Review test account for Play's App access declaration (done 2026-09-08: Google account
      "Markera Review", signed in and tested by the user; credentials live only in Play Console).

## Tasks

| # | Task | Status |
|---|------|--------|
| 1 | `server/`: Ktor backend (auth google/apple/dev, series POST/GET, health), SQLite, tests, Dockerfile + compose | done |
| 2 | Mac mini: install Colima + docker, clone repo, `docker compose up -d`, autostart, verify `/health` over LAN | done |
| 3 | App common: `Caliber` enum, `SeriesApi` client + DTOs, backend token store, unit tests (MockEngine) | done |
| 4 | App Android: sign-in (`camera` flavor Google via Credential Manager, `mock` flavor dev auth), account row on Home, persisted session | done |
| 5 | App: auto-save flow (controller emits series → saver → caliber dialog → POST), caliber badge in viewport, save status; emulator end-to-end against the Mac mini | done |
| 6 | iOS: Apple sign-in actual (AuthenticationServices), ATS exception; compile on the Mac mini | done (compiled + linked only; no iOS host app yet) |
| 7 | App Android: series history screen (Home card → list of saved series: time, caliber, total, per-hole scores; loading/empty/error/signed-out states) | done |
| 8 | Image upload: server `POST/GET /series/{id}/image` (raw JPEG on the volume, `hasImage` in the DTO); app posts the scanned snapshot (JPEG ≤1024 px) after a successful series save; thumbnails in history | done |
| 10 | Admin web UI in the server (`/admin`): users → their series → series details with holes + image; server-rendered HTML, no login, enabled only by `ADMIN_UI=true` (LAN-only deployment); tests; deploy on the Mac mini | done |
| 11 | App: defer the auto-save until the user leaves the frozen frame ("Tillbaka till kamera" in free marking; moving on from a scanned lane in the wizard); rescans discard; caliber dialog moves to that moment. Same commit (amended 2026-09-06): the caliber chip shows only the caliber, moves out of the viewport to the left of the total score, equal height with it; save outcome (and the signed-out hint) becomes a Toast | done |
| 12 | App: score pickers stop showing the previous/next value; tapping a score opens a dialpad-style dialog (0–10 + X) that sets it (queued 2026-09-06 behind task 11, same results area) | done |
| 13 | Server: users get a `name` (Google: `name` claim, else `email`; Apple: `email`; dev: the subject), refreshed at every login, shown on the admin pages | done |
| 14 | App: caliber dialog rows half as tall, "Ingen vald" for no caliber, dialpad order 0–10 then X (orchestrator-applied, 2026-09-06) | done |
| 15a | Server: holes gain `detected_ring`/`detected_inner_ten` (nullable) and `x`/`y`/`distance_mm` become nullable (table rebuilt in the migration); DTO `Hole` mirrors it with defaults so old clients still post; admin series page marks edited holes (detected → chosen) and the series lists show an edited count; tests; deploy | done |
| 15b | App: `SeriesRecorder.commit(topScores)` merges the picker values into the pending holes per the pinned decision (`SeriesDtos` helper, unit-tested); `MarkeraScreen` (Spara) and the wizard (lane change) pass the current `topScores`; phone check that an edited series shows as edited on `/admin` | done |
| 16 | App: remove the "Dela" share button (nothing to share for now); orchestrator-applied | done |
| 17 | Admin UI: whole table rows clickable wherever a row has exactly one target page (users → user, series → series) | done |
| 18a | Admin series page: draw the holes on the photo (positions are in source-image px of the uploaded JPEG, same aspect, so scale by the rendered size), detected and manual in different colours, with the confirmed score as label; typed holes have no position and stay list-only; the image upload takes `?width=&height=` (the scored frame's size) so markers scale onto the JPEG | done |
| 18b | App: manual marking — a tap on the frozen frame (not on an existing marker) adds a `manual` hole at that image point, scored with the current centre + ring, appended to `scores`, filling the next free picker slot, re-emitted to the recorder as the pending series; overlay draws manual markers in their own colour; no tap effect without geometry; the image upload sends `?width=&height=` (source frame) | done (phone-verified 2026-09-07: orange marker, saved as `manual`, markers drawn on `/admin/series/30`) |
| 19 | App: "Återställ" button beside "Spara" on the frozen frame (requested 2026-09-06 as "next to Detektera" — Detektera only exists in the live view, the frozen view shows Spara in its place): discards the scan (`recorder.clear()`, `clearResults()`, unfreeze, back to the live camera) without saving, for when the geometry/detection went wrong (orchestrator-applied) | done |
| 20 | App: hide the "Tävling" (competition) entry on Home until the user reprioritises it (requested 2026-09-06); the code stays, only the entry point goes (`SHOW_COMPETITION` in `AppNavHost`) | done |
| 21 | Home texts: "Fri markering" → "Markera", "Skanna en tavla utan tävling" → "Scanna en tavla" (orchestrator-applied with task 20) | done |
| 22 | Admin: drop the "created" column from the users list (orchestrator-applied, 2026-09-06) | done |
| 23 | Mock flavor: no auto-scan after Spara/Återställ (new image, wait for Detektera) so the flow mirrors the camera; first frame and preview tap still auto-scan (orchestrator-applied, 2026-09-07) | done |
| 24 | Server: lock-down + deletes + pagination — `ADMIN_PASSWORD` basic auth replaces `ADMIN_UI`; `DELETE /series/{id}`; `DELETE /account`; `GET /series?limit&before`; tests; Mac mini `.env`: `ADMIN_PASSWORD=…` set on deploy, `DEV_AUTH=false` flipped 2026-09-07 after 25b | done |
| 25a | App: history pagination (loads the next page when the list end is reached) and series delete (long-press a row → confirm dialog → `DELETE`, row disappears) | done — verified on the phone 2026-09-07 (series 12 deleted, 5 left) |
| 25b | App: account deletion — "Radera konto" on Home under the signed-in row, confirm dialog naming what goes, `DELETE /account`, then sign out; camera + mock | done — verified on the phone 2026-09-07 with the dev account (user 3 wiped) |
| 25c | App: remove a manual marker by tapping it again; picker slots shift; recorder re-published | done — verified on the phone 2026-09-07 (mock flavor, orange marker added then removed) |
| 26 | Release signing: upload keystore + `keystore.properties` (gitignored), `signingConfigs.release` wired when the file exists, upload-key SHA-1 `56:A5:24:7E:75:96:CB:58:48:D9:63:F4:6E:AA:71:F6:36:B3:98:A2` (valid to 2054, alias `markera`), release APK verified signed (orchestrator-applied 2026-09-07) | done |
| 27 | Store assets under `store/`: Swedish short/full description, 512 px icon, 1024×500 feature graphic, phone screenshots; launcher icon replaced (target rings, adaptive vector + legacy webps from `store/gen_assets.py`) | done — a results-screen screenshot with a real target is still wanted |
| 28a | Server: `PUT /series/{id}` (owner only, body = the POST body, replaces caliber/timestamp/holes → 204) + `detectedX`/`detectedY` on holes (migration + backfill from x/y where detected_ring is set); admin kind shows "moved" | done 2026-09-07, deployed |
| 28b | App: series detail screen from a history row — photo with markers, time, caliber, total, hole list (score, mm, kind); tap a score to edit it, drag a marker to move it; Spara → PUT; history refreshes | done — verified on the phone 2026-09-07 (series 11: 10 → 9 + moved) |
| 28c | Admin web: the series page becomes editable — score inputs per hole, drag a marker to move it, click the photo to add a hole, delete per row, Save → `PUT /admin/series/{id}` (Basic auth; same body/logic as the app PUT) | done 2026-09-07, deployed |
| 29 | Admin: score split into read-only `detected` and editable `manual` columns; manual empty = use detected; detected greyed out when overridden; marker labels 13 px; photo 960 px right of the table (requested 2026-09-07) | done, deployed |
| 30 | App: detail screen score split — read-only detected cell (struck through when overridden) + tappable manual cell; dialpad gains "Använd detekterad" to revert to the detected score (requested 2026-09-07) | done — verified on the phone 2026-09-07 (series 11 reverted 9 → 10, saved) |
| 31 | Server + admin: store the target geometry per series — digit-row centre and the 6/7 ring ellipse (`geometry: {centreX, centreY, ringCx, ringCy, ringSemiMajor, ringSemiMinor, ringRotationRad}`, source-image px, nullable, on POST/PUT/GET/admin); admin photo draws the centre and the ring; moving/adding a hole recomputes `distanceMm` from the geometry (JS port of the `scoreHits` un-projection) instead of nulling it (requested 2026-09-07) | done 2026-09-07, deployed — verified in Chrome on series 32: ring + centre drawn, dragging a marker recomputed 48 → 66 mm ("moved"); the JS mm matches the app's stored value to 7 digits |
| 32 | App: send the geometry with every scan (`onSeriesDetected` gets centre + ring), draw centre + ring on the detail photo, recompute `distanceMm` on drag from a shared pure `distanceMm(x, y, centre, ring)` in `vision/` that `scoreHits` also uses; old series without geometry keep nulling (requested 2026-09-07) | done 2026-09-07 — verified on the phone with series 31 (ring + centre drawn; dragged hole 32 → 78 mm, "flyttad", saved and read back 77.9 mm) |
| 33 | Admin: no "Add hole" button when the photo is placeable (clicking the photo is the only way in, so no empty typed row is left behind); photo margin/border moved onto `.shot` so a click, the markers and the ring all map to the exact image pixels — the new hole is centred on the pointer tip (requested 2026-09-07, orchestrator-applied) | done, deployed |
| 34 | App detail screen: tap the photo to add a hole (scored from the geometry when present, else dialpad), delete icon per row (Spara disabled with no holes), pinch zoom + pan on the photo with hole drag still working while zoomed (requested 2026-09-07) | done 2026-09-07 — add (tap at ring 8 → "8, 53 mm, manuell", total 48 → 56) and delete verified on the phone; **pinch zoom awaiting the user** (adb cannot inject multitouch on the unrooted phone) |
| 35 | App detail screen: tighter row spacing in the hole list; the kind column says "detekterad" for an unedited detected hole (today it is blank while hand-placed ones say "manuell") (requested 2026-09-07) | done — verified on the phone 2026-09-07 |
| 36a | Statistik model (commonMain, JVM-tested): `targetOffsetMm` (un-project + rotate back, `distanceMm` = its hypot), `series/stats/SeriesStats.kt` — `StatsFilter` (caliber, from/to, exact hit count, default 5), `plotSeries` (geometry required, positioned holes, sorted oldest→newest with `age` 0..1), `statistics()` (mean distance to centre, mean hole-to-hole, mean score, mean group size, mean point of impact, tens share, best/worst). Plan: `~/.claude/plans/compiled-spinning-boole.md` (requested 2026-09-07) | done 2026-09-07 (134 tests) |
| 36b | Statistik screen (androidMain): Home card + `Screen.Statistics`; loads all pages; filter chips (caliber, date presets 7/30/365/alla + DateRangePicker, hits stepper); drawn target to ring 5 with hits coloured old (blue) → new (green) + legend; measurements list (mm whole numbers, score 1 decimal) | done — verified on the phone 2026-09-07 (series 31: plot matches the photo, 34 mm mean, 48.0, 60 % tens; hits stepper, presets and the custom range picker all work) |
| 37 | Statistik screen: draw the mean and the median point of impact on the target (distinct markers + legend entries); add `medianXMm`/`medianYMm` (component-wise medians) to `SeriesStatistics` and list the median under the mean in the measurements (requested 2026-09-07) | done — verified on the phone 2026-09-07 (2 series: + at -17/16 mm, × at -23/18 mm, both drawn where the numbers say) |
| 38 | First Google Play release (`/release`, camera flavor): add the Play Publisher plugin + `play {}` block, bump 1.0 (1) → 1.1.0 (2), Swedish release notes, `bundleCameraRelease`, upload to the internal track, tag `v1.1.0`. Blocked on the user: `play-account.json` (Play Console → API access → service account "Release manager"); the Play App Signing SHA-1 must be in the Android OAuth client or Google sign-in fails on the Play build (requested 2026-09-07) | bundle 1.1.0 (2) built + tagged v1.1.0; build 2 uploaded by hand, rejected for 16 KB pages (→ task 39); 1.2.0 (3) built + tagged v1.2.0, 1.2.0 (3) uploaded to the internal track via the plugin 2026-09-07 after the user granted the service account release rights. Review test account created and tested by the user 2026-09-08 (Google account "Markera Review", credentials only in Play Console → App access). App signing SHA-1 → Android OAuth client done 2026-09-08 (sign-in verified on the Play build). 1.3.0 (4) uploaded to the internal track 2026-09-08 (tag v1.3.0: R8, portrait lock, full-res capture, cache, export; fresh Statistik/Historik screenshots pushed with the listing). 1.4.0 (5) uploaded 2026-09-08 (tag v1.4.0: tasks 63-68 — Statistik filters/chips/markers, help on every screen, delete confirmations, bold caliber; all four screenshots retaken). Still on the user: Data safety Send for review, promote 1.4.0; 1.5.0 (6) uploaded 2026-09-08 evening (tag v1.5.0, notes since v1.4.0 which is now live) |
| 39 | Play rejected build 2: "does not support 16 KB memory page sizes" — `libonnxruntime.so` 1.19.2 is 4 KB-aligned (every other .so is fine). Bump `onnxruntime-android` to 1.29.0 (16 KB aligned on all ABIs), re-verify detection on the phone, then release 1.2.0 (3) (requested 2026-09-07) | done — mock flavor on the phone: inference 5.6 s, raw=3 kept=3 digits=9 ring=true, no crash |
| 40 | Store listing via the Play Publisher plugin: `composeApp/src/main/play/` (the plugin ignores `androidMain/play`, release notes moved too) — sv-SE title/short/full description, icon, feature graphic, `store/crop_screenshots.py` for phone screenshots (Play needs height ≤ 2× width). `publishCameraReleaseListing` (requested 2026-09-07) | done — listing + 4 screenshots (hem, serie, statistik, historik) published 2026-09-07 |
| 41 | Admin UI: delete a series — `DELETE /admin/series/{id}` (Basic auth, same as PUT; drops holes, row and image) + a "Delete" button on the series page with a `confirm()`, redirecting to the owner's user page; ApiTest coverage (requested 2026-09-07) | done — 44 server tests; deployed to the Mac mini 2026-09-07 |
| 42 | App: mirror the admin delete on the series detail screen — a visible "Radera" action (top bar or beside Spara) with the existing `history_delete_*` confirm dialog, `api.deleteSeries`, then back to a history list that no longer shows the series (long-press delete in the list stays) (requested 2026-09-07) | done — verified on the phone 2026-09-07 |
| 43 | Statistik screen polish: "Välj…" chip moves onto the hits row; − and + become real icon buttons placed left of the "N träffar" text; tighter spacing between the filter rows; ring digits 5–9 also drawn along the vertical axis of the target (requested 2026-09-07) | done — verified on the phone 2026-09-07 |
| 44 | Statistik: every filter row starts with an "Alla" chip, selected by default — date presets reordered (Alla first, already the default); hits row gets an "Alla" chip left of −/+ meaning any hit count (`StatsFilter.hits: Int? = null`, null = no count filter; tapping −/+ selects a count, tapping Alla clears it); caliber row already has it (requested 2026-09-07) | done — verified on the phone 2026-09-07 (136 tests) |
| 45 | Scan screen top bar reported missing ("we dont show the top bar with the title and back arrow") — on the debug build 2026-09-07 the phone shows ← + "Markera" + debug toggle in both live and frozen states (screenshots `scan45*.png`); needs the user to say where it is missing (Play build 1.2.0? landscape? permission prompt has no bar) (requested 2026-09-07) | done 2026-09-07 (verified on the phone): the live preview was a SurfaceView, which punches its own window hole and ignores clipping, so the fill-cropped 4:3 frame spilled over the top bar; PreviewView now `COMPATIBLE` (TextureView) + `clipToBounds` on the preview box |
| 46 | Scan screen: same hole editing + pinch zoom as the series detail — pinch/pan on the frozen frame, drag any hole to move it (rescored via the geometry; a moved detected hole keeps its detected ring/position), long-press a hole to delete it, tap empty to add (existing); the pending series and the pickers follow every edit (requested 2026-09-07) | done 2026-09-07 (gate 141 green; phone check pending — phone not connected): `PhotoGestures.kt` shared with the detail screen, `HitScore.original`, `moveHit`/`removeHit` |
| 47 | Scores only from hole position: remove score editing by tapping the digit/picker (scan screen, series detail, wizard?) — a score changes only by moving a detected hole or adding + positioning a new one, then it is computed from the geometry, so every saved score is accurate for training (requested 2026-09-07) | done 2026-09-07 (gate 143; detail drag → "9 → X, flyttad", re-sort, no dialpad verified on the phone; scan-screen drag needs a target in front of the camera): sort lives in `MarkeraViewModelImpl.withHoles` and `SeriesDtos.withNewHole/moveHole`; `scoreHits` now keeps input order (fixes a 46 bug: detections/scores were misaligned) |
| 48 | Statistik screen: a "?" (top bar action) opening a dialog that explains each value shown (mean distance, hole-to-hole, group size, mean/median point of impact, tens share, best/worst, colour scale) (requested 2026-09-07) | done 2026-09-07 (gate 143; phone check with 49/50): `HelpDialog` in `StatsScreen.kt`, `stats_help_*` strings |
| 49 | Statistik: the mean and median markers in the target image must be the same size and style as in the legend (requested 2026-09-07) | done 2026-09-07 (gate 143; phone check with 50): shared `drawMark` in dp for target + legend, × arms normalised to the + extent |
| 50 | Statistik: widen the date colour scale of the hits and make it pop against the white/black target (requested 2026-09-07) | done 2026-09-07 (gate 143; 48-50 verified on the phone, `stats48.png`/`stats50.png`): five-stop `HIT_SCALE` violet→blue→cyan→yellow→orange-red, same list feeds the legend bar |
| 51 | Scan screen: after Detektera with fewer than 5 holes, a press on the photo adds a hole there; holes drag and drop. Tap-add exists since task 33 and drag since 46 (fca2b58) — open: is "fewer than 5" a hard cap on adding? (requested 2026-09-07) | done 2026-09-07 (gate 143; user: "adding holes should stop at 5"): tap-add refused at `SCORE_PICKER_COUNT` holes in `addManualHit` and the detail `onAdd`; the "sixth manual hit" test replaced by `addManualHitStopsAtFiveHoles` |
| 52 | Keep the score digits sorted (highest first) whenever any value changes — after a hole is moved, added or removed, on the scan screen and the series detail (requested 2026-09-07) | done with 47 |
| 53 | Long-press deletes a hole and its digit goes back to the default (0). Already so since 46: `removeHit` → `withHoles` re-sorts and refills the pickers with 0 (requested 2026-09-07) | done (covered by 46/47) |
| 54 | Delete the old series without geometry (pre 2026-09-07 test data) from the production DB — user: "old series without geometry can be deleted, we dont need old test data"; no rescan path needed (requested 2026-09-07) | done 2026-09-16 — the tally in this row was stale: only **5** geometry-less series were left (ids 7-11, all 2026-09-06, user 7); ids 1, 4-6 and 13-16 no longer exist in the DB at all. Four of the five (7-10) were already soft-deleted and hold 0 holes, so the only live one was **11** (5 holes, `images/11.jpg`) — deleted via `DELETE /admin/series/11` (204) after backing the DB up to `~/markera-backup-2026-09-16-1849.db` on the Mac. That route is the app's own soft delete (`db.deleteSeries`): hidden from every read and pushed to the clients as a tombstone, with holes + photo kept as training data by design (task 90) and restorable from the admin page. 33 visible series remain. Rows 7-10 were deliberately left in place: 0 holes, no images, and purging them would only drop tombstones clients may still need. The classifier allowed the admin DELETE this time (ADMIN_PASSWORD read from the container env, never printed) |
| 55 | Delete the mock flavour ("we dont need it anymore"): drop the `source` flavor dimension, `androidMock`/`src/mock`, `prepareMockFrames`, the dev sign-in, fold `androidCamera`/`src/camera` into the main source sets; task names lose the flavor (`assembleDebug`, `testDebugUnitTest`, `bundleRelease`, `publishReleaseBundle`); update CLAUDE.md, README, `/deploy`, `/release`; server `POST /auth/dev` stays (gated by DEV_AUTH) (requested 2026-09-07) | done (2026-09-07) |
| 56 | Release hardening: R8 minify + resource shrinking on `release` with keep rules for ONNX Runtime, ML Kit, kotlinx-serialization DTOs, Ktor, Credential Manager/googleid; native debug symbols in the bundle (`ndk.debugSymbolLevel`) so Play shows symbolised crashes; mapping upload via the Play plugin; verify a release build on the phone end to end (scan, sign-in, save) (requested 2026-09-07) | done (2026-09-07; release build verified on the phone after a second keep rule for the ML Kit registrar constructors) |
| 57a | Local cache, server side: `updated_at` + soft-delete `deleted_at` on `series`, `GET /series?since=` delta with tombstones, paged list ordered by id (cursor bug), `updatedAt`/`deleted` on the wire, ApiTest cover, deploy (plan `~/.claude/plans/compiled-spinning-boole.md`; requested 2026-09-07, user chose read-cache + delta refresh in SQLite) | done (2026-09-07; 49 server tests; DELETE /account still hard-deletes since sessions die with it) |
| 57b | Local cache, app side: SQLDelight store (series json + sync stamp), `SeriesRepository` (StateFlow + `refresh()` delta + image file cache in `cacheDir`) hoisted in `AppNavHost`; History/Statistik/Detail read it, writes go through it, sign-out clears it; JVM tests with in-memory driver + ktor mock | done (2026-09-07; 151 tests; phone-checked 2026-09-08: second Historik open rewrote no cached image, Historik + Statistik work in airplane mode, a Detail edit and a delete show in the list at once and the sync stamp advances; sign-out clearing covered by the JVM test only) |
| 58 | Export from Historik: "Exportera" builds one ZIP (`series.csv`, `holes.csv`, `images/<seriesId>.jpg`) in `cacheDir` and shares it through `ACTION_SEND` via a FileProvider content URI (`application/zip`); CSVs semicolon-separated with a header, UTF-8 BOM so Excel opens them; data from the local cache once 57b lands (else from the API pages) (requested 2026-09-07) | done (2026-09-07; 155 tests; phone-checked 2026-09-08: share sheet opens with `markera-export-<stamp>.zip`, zip holds series.csv/holes.csv/images) |
| 59 | Full-resolution scan capture: CameraX `ImageCapture` cropped to the square viewport (~3000² on the OnePlus) feeds both detection and the saved JPEG (`IMAGE_MAX_DIM` 3072, q90, server `MAX_IMAGE_BYTES` 10 MB); `FrameSource.capture()` suspends, preview bitmap only as fallback; History/Detail decode with `inSampleSize`. Reason: training photos are 12 MP (hole ~80 px), the 1024 px upload gives ~18 px (plan `~/.claude/plans/compiled-spinning-boole.md`; requested 2026-09-07, do before 57a/b) | done (2026-09-07; phone shows snapshot 3000x3000, capture 797 ms, OCR 288 ms; accuracy on a real target awaits the user) |
| 60 | Screen rotation must not reset navigation: the sealed-class back stack in `AppNavHost` (and the frozen scan/result state) has to survive the Activity recreation, e.g. `rememberSaveable` for the stack or `configChanges` for orientation (requested 2026-09-08, seen on the phone: rotating on the scan screen lands on Home) | done (2026-09-08; `android:screenOrientation="portrait"` on MainActivity — the layout is portrait-only and the camera viewport is bound with one rotation, so a rotation never recreates the Activity; phone reports the portrait request and adb cannot force a rotation on the OnePlus, so a hand rotation by the user is the final check) |
| 61 | Public account-deletion page on the backend for the Play listing: `GET /delete-account` (no auth) serves a small static HTML page, Swedish + English, saying how to delete in the app (Home → Radera konto), what goes (account, every series, image, session; immediately, nothing kept) and how to ask by mail when the app is unavailable (`CONTACT_EMAIL` env, line omitted when unset); ApiTest cover; deploy (requested 2026-09-08) | done (2026-09-08; 51 server tests; live at `https://markera.duckdns.org/delete-account`, no `CONTACT_EMAIL` set so the page points at the Play listing's developer e-mail) |
| 62 | Privacy policy served by the backend (`GET /privacy`, Swedish + English, shares the HTML shell with `/delete-account`) describing what Markera really stores — Google sign-in subject + display name, saved series (time, caliber, holes/scores), target photos, session tokens; on-device OCR/detection, no analytics/ads; own server in Sweden; kept until the user deletes (in-app or `/delete-account`); export via Historik; rights + IMY — replacing the generic flycricket policy in Play Console (requested 2026-09-08) | done (2026-09-08; 53 server tests; live at `https://markera.duckdns.org/privacy`; the user pastes it into Play Console → App content → Privacy policy) |
| 63 | Statistik filter rows: the date-range chip (`Välj…`) moves up onto the date row after `1 år`; `7 dagar`/`30 dagar` become `7 d`/`30 d`; the hit-count row loses the +/- buttons and the count text — it is just `Alla` and a `5 träffar` chip, so the hole-count filter is either five or all (requested 2026-09-08) | done (c8b3766; phone-checked 2026-09-08) |
| 64 | Help icon (?) in the top bar of every screen, like Statistik's, each opening a dialog with useful per-screen guidance: Home (what the app does, sign-in, caliber, Radera konto), scan screen (aim, scan, tap to add / long-press to delete / drag, pinch zoom, Spara, caliber), Serie/Detail (edit holes, delete series), Historik (list, export zip, tap for details) (requested 2026-09-08) | done (ef6d669; shared `ui/HelpDialog.kt`, Swedish sections per screen, texts checked against the code; phone-checked on Home) |
| 65 | Statistik chips: a selected chip must stand out clearly — today the outline on unselected chips makes them look almost selected; give selected chips a solid accent fill (and drop or fade the border on unselected ones) (requested 2026-09-08) | done (2026-09-08; primary fill + onPrimary label when selected, surfaceVariant pill without border otherwise; phone-checked) |
| 66 | Statistik: the Medel (+) and Median (×) markers must use a colour nowhere near the hole date scale (purple→cyan→green→yellow→orange→red) — white with a thin dark outline, on the target and in the legend alike (`drawMark`) (requested 2026-09-08) | done (2026-09-08; both `Color.White` over the existing dark halo; phone-checked) |
| 67 | "Are you sure" dialog on every delete: account and series deletion already confirm; add one shared `DeleteHoleDialog` for the Serie screen's per-row trash button and the scan screen's long-press hole removal (requested 2026-09-08) | done (e339b5e; phone-checked on the Serie screen: dialog, Avbryt keeps the hole) |
| 68 | Historik rows (and the matching date/caliber header on the Serie screen): the caliber must stand out — same text size as the date and bold (requested 2026-09-08) | done (ab01d2a; titleMedium + bold on both screens; phone-checked) |
| 69 | Statistik: spread measures that ignore the target centre — alongside the existing Medelavstånd mellan träffar / Gruppstorlek add Medelradie (mean distance from each series' own group centre, averaged over series) and Spridning (radial SD of all selected hits around their mean impact point), with help texts and unit tests (requested 2026-09-08) | done (2026-09-08; 158 tests; not phone-checked — the app is uninstalled from the phone at the user's request) |
| 70 | Hole letters a–e: after detection, each hole marker in the overlay and its score box in the picker row carry the same small letter (clearly smaller than the score digit) so the user sees which digit belongs to which hole; the same letters on the Serie detail page (overlay + hole rows) (requested 2026-09-08) | done (2026-09-08; 158 tests; phone-checked on a Serie page: letters on markers and rows, no row wrap; user confirmed the scan screen on a live scan) |
| 71 | Dragging a hole on the photo (scan + Serie page) holds it about 1 cm above the finger, so the finger does not cover the spot being placed (requested 2026-09-08) | done (2026-09-08; one change in `PhotoGestures.kt`; phone-checked: a 400 px swipe left the hole ~1 cm above the finger) |
| 72 | Serie page: the Spara button sits right below the photo and stays greyed out until a hole was changed (requested 2026-09-08) | done (2026-09-08; the button was already disabled while unchanged, only moved) |
| 73 | iOS version of the app (requested 2026-09-09): full parity on the simulator, shared Compose UI, bundle id `se.kjellstrand.markera`, competition wizard stays Android-only. Design + per-task status in `PLAN-IOS.md` | done 2026-09-10 on the simulator (C0–D1 in `PLAN-IOS.md`; commits 5b0a230…c4129b3); 1.5.0 (6) on TestFlight 2026-09-10; the device camera path is untested (no iPhone) |
| 74 | Deleting a hole on the Serie screen keeps it in the backend for training: the PUT sends it flagged `deleted: true`, the server stores it (`holes.deleted`, migrated in place), never reads it back and leaves it alone on a later wholesale replace; the app cache filters it out of its own update (requested 2026-09-11) | done 2026-09-11 (d6ed647; server deployed to the Mac mini; scan-screen long-press still just drops the hole — picker 0 is the false-positive signal there) |
| 75 | Statistik tabs: "Tavla" keeps the hit cloud on the target, "Trend" charts any of the measurement rows over time — metric chips, x-axis bucketing (per serie/dag/vecka/månad, a bucket = the group statistic over its series), "Dela per kaliber" for one line per caliber, the shared filter rows above both tabs, tap a point to read it off (requested 2026-09-11) | done 2026-09-11 — `series/stats/Trend.kt` + `ui/stats/TrendChart.kt` (Canvas line chart, CVD-validated palette, colours by caliber slot), `TrendTest`; phone-checked (split lines, readout). Same day: the chart became a scatterplot with a least-squares trend line + dashed mean per series of marks (`TrendFit`), the impact metrics left the Trend tab and "Andel tior" left the app entirely |
| 76 | Statistik: the mean (+) and median (×) markers get distinct strong colours far from the hit colour scale — magenta and green, the two hue gaps the scale leaves (requested 2026-09-15) | done 2026-09-15 (palette-validated against the hit scale; phone-checked 2026-09-15) |
| 77 | Historik filters: multi-select caliber chips (Alla + every caliber present, including –, OR within the group) and the Statistik date chips (Alla / Välj… / 7 d / 30 d / 1 år), AND between groups; informative empty text; the selection persists across navigation and restart (token store); export zips only the filtered series and is disabled when nothing matches (requested 2026-09-15) | done 2026-09-15 — `ui/history/HistoryFilter.kt` (filter, codec), `HistoryFilterTest`; date window shared with Statistik via `DatePreset.window`; persisted as a string in `BackendTokenStore` (survives sign-out like the caliber); iOS framework compiled on the Mac; phone-checked 2026-09-15 (9mm chip narrows the list) |
| 78 | Statistik: the hit age scale (hits + the bar under the target) becomes one continuous gradient between two far-apart colours, blue #304FFE (oldest) → yellow #FFEA00 (newest), so early and late series are easy to tell apart; the magenta/green markers stay (validated far from both ends) (requested 2026-09-15) | done 2026-09-15 (two-stop `HIT_SCALE`; gate green; phone-checked 2026-09-15) |
| 79 | Statistik: a titled divider (`——— title ———`) above each chip group — Kaliber, Datum, and on the Trend tab Mått and Gruppering (requested 2026-09-15) | done 2026-09-15 (`SectionDivider` in `StatsScreen.kt`; phone-checked on both tabs) |
| 80 | Historik: changing a filter scrolls the list back to the newest series — today clearing the 9mm chip left the list anchored on the 9mm cards mid-list (found on the phone 2026-09-15, follow-up to 77) | done 2026-09-15 (`onFilter` scrolls the list to item 0; gate green; phone check pending, the phone disconnected before the repro ran) |
| 81 | Statistik Tavla: two knobs on the age colour bar pick a start and end series within the filtered range; they snap to the series positions, each series is a small dot on the bar; the target, the mean/median markers and the measurement rows show only the picked segment, hits keep their full-range colours; the dates under the bar follow the knobs (requested 2026-09-15) | done 2026-09-15 (material3 `RangeSlider` with a custom `AgeTrack` in `StatsScreen.kt`; forced gate green, iOS framework compiled; phone-checked 2026-09-15: the start knob snaps onto a dot, the earlier blue hits leave the target and the start date follows) |
| 82 | Historik: task 80's scroll did not hold on the phone — after 9mm on/off the list still sat mid-list, because `onFilter` scrolled the old list before the filtered one was composed and the keyed anchor won; scroll from `LaunchedEffect(filter)` instead (found 2026-09-15) | done 2026-09-15 (gate green; phone-checked: scrolled to the bottom, 9mm on then off, the list opens on the newest series; this also closes 80's pending phone check) |
| 83 | Markera: "Ny ring" retries only the 6/7 ring on the frozen scan — candidates around the digit centre from three radius priors (digit median, `estimateDiskRadius` profile, the 0.35·width viewfinder guide) × radius/band/threshold variations, ranked by rim contrast (dark inside, light outside the ellipse); each press shows the next distinct candidate and rescores the holes; the first scan is unchanged (requested 2026-09-15, plan in `~/.claude/plans/tingly-doodling-ocean.md`) | done 2026-09-15 (`ringCandidates`/`rimContrast` in `RingGeometry.kt`, `retryRing` in `TargetScanner.kt`; `RingCandidatesTest` reproduces a 40%-off seed that `refine67ToEdge` falls back on and the top candidate recovers within 0.03%; ~150-220 ms for a 3000² frame on JVM; forced gate, androidTest + :eval compile and iOS framework green; `BlackRing67Test` run on the phone 2026-09-15 over all 39 ring-eval photos (overlays judged by eye): the top candidate sits on the black rim on 36 of the 37 with a centre (miss: 20260614_123422, on the paper), while today's first-scan `refine67ToEdge` ring is right on only ~3 (mostly locked inside the disk or squashed) — the rim-shape refine from 24d9282 had never been device-evaluated; candidates took ~560-970 ms per 1504×2000 photo on the OnePlus. Button itself not yet tried on a live scan. Open: the cached frame + grayscale stay in memory until the next scan; a dragged hole keeps its `original` scored against the first ring) |
| 84 | Markera: let the first scan use the top `ringCandidates` result (falling back to `refine67ToEdge` when there is none), given task 83's eval (candidate right on 36/37, first-scan refine on ~3/37); needs the timing looked at (~0.6 s extra per scan on the OnePlus) and a live check at high tilt (queued 2026-09-15, awaiting the user's go-ahead) | dropped 2026-09-15 — superseded by the probe-disk ring (86/87); `ringCandidates` removed in 88 |
| 85 | Markera: tap a hole's score box after the scan to set its score with the dialpad, without moving the hole — partly reverses 47 at the user's request (2026-09-15); the hole keeps the detector's score as `original`, so the saved hole reads detected 9 → confirmed X; the typed score survives re-sorts, other hole edits and Ny ring, and moving that hole returns it to the position score | done 2026-09-15 (`HitScore.typed`; `withTypedScore`/`movedFrom`/`rescoredFrom` in `ManualHit.kt`; `TargetScanController.setScore` via `editHoles`; portrait score boxes with a hole open the dialpad, empty slots and the landscape column stay read-only; `TypedScoreTest` 6 tests; forced gate + iOS framework green; NOT yet on the phone — disconnected. Open: after Ny ring a typed or moved hole keeps the old ring's detector score as its detected value); live-tested on a real target 2026-09-16 (user: works as expected) |
| 86 | Markera: probe-disk 6/7 ring fit, evaluated as overlays only (user request 2026-09-15) — four start points on the 6/7 boundary along the digit rows (per-side distance from the 6–9 digits), a 50 px (adjustable) disk at each moved in/out by its dark/light ratio until it sits on the rim, then an ellipse exactly through the four disk centres; `vision/RingProbe.kt` + `RingProbeTest`, `BlackRing67Test#probeRings` renders overlays + zoom sheets for the phone's cached series images plus the server (39) and dev (3) series images copied to `D:/ml/ring-probe`, output in `D:/ml/ring-probe/overlays`; the scan path is untouched until the user has judged the overlays | code done 2026-09-15 (RingProbeTest 3/3, forced gate + test APK + iOS framework green; dark = at or below Otsu, side digits within 45° of the ray); overlays rendered 2026-09-15 on the OnePlus for 42 images (25 cached + 39 server + 3 dev, dev-3 = series 37): centre and ellipse on all 42; by eye every probe sits on the rim except dev-2's left one (digit start ~90 px short, travel cap stopped it ~15 px inside the black, dark 0.83) and dev-1's top one (off-image, NaN; the ellipse still follows the visible rim); 2 iterations typical; awaiting the user's verdict |
| 87 | Markera: fix task 86's dev-2 miss (a probe whose digit start is far off hit the half-ring-width travel cap while still mostly dark) and wire the probe-disk fit into the scan in place of `refine67ToEdge`, keeping the digit-seed refine as the fallback when the probe ellipse is missing or implausible (user request 2026-09-15) | done 2026-09-15 (travel cap one ring width; `fit67Ring` in `RingProbe.kt` = probes if plausible, else seed → refine; `runPipeline` logs `ring PROBES/REFINE`; RingProbeTest 6/6, forced gate + test APK + iOS framework green; on-phone rerun over 42 scans: PROBES chosen on all 42, dev-2's left probe now on the rim (dark 0.497, travel 103 px); installed on the OnePlus, not yet tried on a live scan) |
| 88 | Markera: keep only the probe-disk ring (user 2026-09-15: "new ring detection is great… discard the older versions") — remove the "Ny ring" button and `ringCandidates`, drop the `refine67ToEdge` fallback (implausible probe ellipse = no ring, the user's choice), delete the unused calibration/RANSAC/blob/edge-fit files and their tests and eval mosaics; docs updated | done 2026-09-15 — 29 files, −2287 lines; gate 187 tests green, eval compiles, iOS links; live-tested on a real target 2026-09-16 (user: "both works as expected") |
| 89 | Markera: label the viewfinder guide circle in the live camera preview with a 7 just inside and a 6 just outside it (user request 2026-09-15), so it is clear the green circle is meant to sit on the 6/7 ring | done 2026-09-15 — gate 187 green, iOS links; phone-checked 2026-09-16 |
| 90 | Server admin web UI: see and change a series' deleted state (user request 2026-09-15) — the user page lists soft-deleted series greyed with their deletion time; a deleted series' page is read-only with a Restore button (`POST /admin/series/{id}/restore` clears `deleted_at`, bumps `updated_at`, so the app's delta brings it back). User's choice: deleting a series now keeps its holes and photo on the server (only account deletion removes them), privacy + delete-account pages reworded, effective date 2026-09-15. Series deleted before this lost their holes/photo and restore empty | done 2026-09-15 — server tests 57 green; live 2026-09-16 (the user ran the redeploy; /privacy and /delete-account serve the new wording) |
| 91 | Statistik: move the Kaliber and Datum filter sections above the Tavla/Trend tabs (user request 2026-09-16) — they apply to both tabs, so they read as shared; the tab-specific sections (Mått, Gruppering, the Tavla range knobs) stay under the tabs | done 2026-09-16 — gate 187 green, iOS links; phone-checked 2026-09-16 (both tabs) |
| 92 | Statistik Tavla date bar (user request 2026-09-16): gradient ends in red instead of yellow, the range knobs become shorter/stubbier/rounder, and the start/end date labels move above the line | done 2026-09-16 — gate 187 green, iOS links, phone-checked 2026-09-16; user's choice: the whole age scale ends in red, so the hit dots match |
| 93 | Historik: group the list by day with a divider per day, days collapsible, list starts collapsed showing only the days with activity (user request 2026-09-16) | done 2026-09-16 — gate 190 green (3 new grouping tests), iOS links, phone-checked 2026-09-16 (collapsed on open, day folds out) |
| 94 | Statistik: move Kaliber and Datum back below the tabs — 91 looked bad on the phone (user 2026-09-16); the state stays hoisted in StatsScreen, so the two tabs keep sharing the same filter | done 2026-09-16 — gate 187 green, iOS links, phone-checked 2026-09-16 |
| 95 | Historik day header: "1 serier" should read "1 serie" (seen on the phone 2026-09-16) | done 2026-09-16 — gate green, iOS links, phone-checked 2026-09-16 (two one-series days read "1 serie") |
| 96 | Help text: the Radera item on the series detail screen still said the bin "tar bort hela serien från ditt konto", which stopped being true with task 90 — it now says the series leaves the app while the server keeps it, its holes and its photo until the account is deleted (user 2026-09-16) | done 2026-09-16 — gate 190 green, iOS links, phone-checked 2026-09-16 (help sheet on a series) |
| 97 | Release 1.7.0 (10) (`/release` 2026-09-16) — notes written against **1.5.0**, the last public release (1.6.0 (9) reached the internal track but was never promoted), so they cover the new ring detection, typed scores, the 6/7 viewfinder labels, Historik day grouping, the Statistik tabs/filters, the icon and the five-hole cap; iOS marketing version + TestFlight notes bumped alongside; the four Play phone screenshots retaken on 34282ee3, and both tablet sets regenerated from them the way the repo has always built them (the phone shot pasted unscaled on an app-background canvas: 7" 1620x2880 at x=90, 10" 2160x3840 at 360,480) — no tablet and no emulator needed; the four App Store screenshots retaken the same day on the iPhone 17 Pro Max simulator (1320x2868, `idb ui tap` in points + `simctl io screenshot`, Debug build against the dev backend) and flattened to RGB, since App Store Connect rejects PNGs with an alpha channel | done 2026-09-16 — AAB built and signed (139 MB), commit 832b729, tag v1.7.0 pushed; the user ran both Play tasks (the classifier denies my uploads): build 10 is in track 'internal' and the four new phone screenshots are live on the sv-SE listing. The user ran `publishReleaseListing` again 2026-09-16, so both tablet sets (7" and 10") are live on the listing too. The App Store side went out the same day: iOS 1.7.0 is on TestFlight as **build 11** (`project.yml` said 10, but ASC had already consumed 10, so the beta lane bumped past it — project.yml now says 11), and `fastlane metadata` pushed the four screenshots + Swedish notes and attached build 11 to version 1.7.0. That push first died with `invalid byte sequence in US-ASCII`: `scripts/mac.sh` sets no locale, so Ruby came up US-ASCII and deliver choked on the Swedish text — hence the new `scripts/mac-metadata.sh`, which exports `LANG`/`LC_ALL` the way `mac-beta.sh` already did. Only the App Privacy questionnaire (ASC UI, no public API) now stands between this and `fastlane release`. Promotion to production is still a manual step in Play Console |
| 98 | App Store `02-serie.png` showed the green 6/7 ring badly misplaced (user spotted it in the live store listing 2026-09-16) | done 2026-09-16 — **not a code bug**: `GeometryDto` is persisted per series (`series/SeriesDtos.kt:89`) and `SeriesDetailScreen:232` draws `series.geometry?.ring()` without ever recomputing, so a screenshot of a 2026-09-10 dev series still showed the pre-task-88 `refine67ToEdge` fit. Measured the old shot from the red centre outward: the ring sat ~106 px (≈20% of radius) inside the black edge on three of four axes, tracking a printed ring line between the 7 and 8 digits. Rescanned the same target on the simulator (Debug 1.7.0 = probe-disk fit) → 49 p (X X 10 10 9); the new ring measures **+2.7 px mean, 18 px worst (3.3% of a 537 px radius)** against the true 6/7 edge over 29 rays. `02-serie.png` replaced (flattened to RGB) and pushed to ASC with the new `scripts/mac-metadata.sh`; build 11 re-attached to 1.7.0. `04-historik.png` was retaken in the same round (user 2026-09-16: it still listed only the old series) — it now shows three day groups, all expanded, with the new 49 p series on top, which also advertises the day grouping better than the old two-day shot. `01-hem.png` and `03-statistik.png` were unaffected. Lesson: a store screenshot must come from a series scanned on the current build |
| 99 | Submit iOS 1.7.0 for App Store review (the user published the App Privacy questionnaire 2026-09-16 and confirmed the submission) | done 2026-09-16 — `fastlane release` on the Mac: screenshots re-uploaded (4 kept, 0 duplicates), build 11 selected, content rights declared, **"Successfully submitted the app for review"**. `automatic_release: false`, so Apple's approval does not publish it — the user releases manually in ASC. Like the other Mac lanes it needs `LANG`/`LC_ALL=en_US.UTF-8`, otherwise deliver dies on the Swedish text. Full log on the Mac at `/tmp/release.log`. Superseded the same day: the submission was pulled back and re-submitted 13:17 so the corrected `02-serie.png` could go in (row 100) |
| 100 | App Store `02-serie.png`: the replacement shot from row 98 framed the target badly — the black disk ran off the photo edge (user 2026-09-16: "you litteraly took one of the worst screenshots") | done 2026-09-16 — reshot on the simulator from a picker image whose whole black centre sits inside the frame: 41 p (9 9 8 8 7), 2026-09-16 13:01. Framing verified numerically rather than by eye — on the shipping PNG all **36 rays out of the red centre reach the black edge, 0 skipped** (the 49 p shot skipped 7, which is exactly the defect the user saw). Ring accuracy on that same file: median **+4 px**, worst +6 px on a 326-379 px radius (1.7%); the one +50 px ray is a light repair patch painted on the black at that angle stopping the edge scan early, not a ring error. The first metadata push was refused with `Failed verification of all screenshots deleted... 4 screenshot(s) still exist`, because 1.7.0 is `WAITING_FOR_REVIEW` (row 99) and Apple locks a submitted version's assets. Not a script bug and nothing was modified on ASC — deliver dies before uploading. The trap: spaceship counts WAITING_FOR_REVIEW among its *editable* states, so `get_edit_app_store_version` succeeds and `dedupe_screenshots` happily lists the set; only the delete fails. New `fastlane state` lane prints each version's real `app_store_state` so this is one command to check. Two new lanes close that gap: `state` prints every version's real `app_store_state`, and `pull_back` cancels the in-progress review submission. The user chose to pull back rather than ship the weak shot — cheap, because the app has never been publicly released (iTunes lookup: not on the store), so it cost queue position only. Cancelled, all four screenshots re-uploaded (0 duplicates), build 11 re-attached, 1.7.0 re-submitted 13:17: **"Successfully submitted the app for review"**, state back to WAITING_FOR_REVIEW with the corrected shot in place. Lesson for the next store shot: pick the picker image by framing first, and treat "rays skipped" in the ring measurement as a framing failure, not a measurement detail |
| 101 | Admin: a "Delete user" button with an are-you-sure dialog (requested 2026-09-16) | done 2026-09-16 — `DELETE /admin/users/{id}` reuses the account path (`db.deleteAccount(id).forEach { imageFile(images, it).delete() }`), so no new deletion SQL: user, sessions, every series (soft-deleted included), holes and JPEGs go for good. The button sits under the user's series table and fires `confirm("Delete user <name> and their <n> series, with every hole and photo? This cannot be undone.")` — worded to separate it from the series delete, which is soft and restorable (task 90). The count is the table's own row count, soft-deleted rows included, because those go too (`UserRow.seriesCount` counts only live ones and would under-report). The name is interpolated into a JS string, so it is JSON-encoded through a new `jsString()` helper (`</` escaped like `blob()`); `ApiTest.adminDeletesAUserOutright` pins that with a user named `quote"and'apostrophe`, plus 401/401/404, and checks the row, the image file and the session are really gone and a second user is untouched. 51 tests green. **Needs a backend redeploy to go live** |
| 102 | Detaljsidan: "Kaliber: " framför värdet, och värdet klickbart så en felaktig kaliber kan rättas (user 2026-09-16: "det står ingen text innan så det är svårt att veta att det är kaliber" + "gör Kaliber klickbar så man kan välja att ändra om man ser det är fel") | done 2026-09-16 — återanvänder `CaliberDialog` (privat i `AppNavHost.kt`, nu `internal`); **inte** via `SeriesRecorder`, som äger den pågående skanningen och alltså hade ändrat fel serie. Valet går till lokal state (`remember(series.id)`) och sparas först med Spara, vars enabled-villkor vidgats till `holes.value != series.holes || caliber.label != series.caliber` — sparar dialogen direkt skickas användarens oskrivna håländringar upp på köpet. `holes.value.isNotEmpty()` ligger kvar före `||`, så en kaliberändring på en serie utan träffar fortfarande inte kan sparas (servern avvisar tom hållista). Mellanrummet efter etiketten är `Arrangement.spacedBy(4.dp)`, inte ett avslutande blanksteg i strängresursen, som parsers trimmar. Följdnotering: `help_detail_*` nämner inte att kalibern går att trycka på — värt en rad nästa gång hjälptexten ändås rörs | 190 tests green |
| 103 | Detaljsidan: ändra en träffs poäng genom att klicka på siffran, som på markera-skärmen (user 2026-09-16: "Jag vill oxå kunna ändra värdet på en träff genom att klicka på siffran. Samma som i markera skärmen") | done 2026-09-16 — återanvänder `ScoreBox` (privat i `ScorePickerRow.kt`, nu `internal`), alltså exakt samma dialpad som markera-skärmen, inte en andra dialog. Fällan: `manualLabel()` är null för en orörd detektion, så cellen visade platshållaren "—" — att rätta en detekterad 8:a hade blivit att trycka på ett streck. Rutan visar därför alltid träffens faktiska poäng. Detektorns egen siffra lämnas orörd (träningsdata); `isEdited()` ger "8 → 9" och överstrykningen automatiskt, så ägarskapet syns fortfarande trots att MANUAL_COLOR försvann ur cellen. Skrivningen (`pickRing`/`pickInnerTen`) går till lokal `holes`-state och sparas med Spara. Grind: `geometry != null`, samma regel som fotoredigeringen — utan geometri är rutan inert. Radens spacing 16→12 dp, för rutan är 4 dp bredare än texten den ersatte och "detekterad" får inte radbrytas. 190 tests green. Noterat, ej ändrat: `detail_no_score` är nu oanvänd, och en poängändring sorterar inte om listan efter `HOLE_ORDER` medan en flyttad träff gör det |
| 104 | Taggar på serier: tagga när serien skapas och när den öppnas i Historik, och filtrera Historik på tagg. Två defaulttaggar: **träning** och **tävling** (user 2026-09-16) | **klar 2026-09-16** — servern: `tag TEXT` på `series` (nullable, så inga rader behöver backfillas), idempotent migrering i `Db.kt`s befintliga stil, `tag` accepteras på `POST`/`PUT` och returneras i `GET /series` inklusive `?since=`-deltat. Fritext utan vitlista — bara längden (32 tecken) är serverns sak, till skillnad från `CALIBERS`. Trimmas, och tomt lagras som null så "ingen tagg" har exakt en representation. Bakåtkompatibelt åt båda håll, vilket är det som gör att servern kan deployas före appen: fältet är defaultat så en gammal app som utelämnar det fortsatt sparar, och klienten bygger på `createWebshooterHttpClient` vars `webshooterJson` har `ignoreUnknownKeys`, så det nya fältet i svaret ignoreras av redan installerade appar. Agenten hittade dessutom ett rotorsaksfel utanför bokstaven i uppdraget: admin-editorns `save()` PUT:ar tillbaka hela serien, så utan `tag` i den bodyn hade varje admin-redigering tyst raderat användarens tagg. 63 servertester gröna. Kvar (apphalvan): designen är bestämd av användaren 2026-09-16: **fritext med träning/tävling som förval**, **en tagg per serie**, och den sätts med ett **chip bredvid kalibern** på markera-skärmen (chipet minns senaste valet, så ett träningspass kostar noll extra klick per serie). Det gör den billig: en `tag TEXT`-kolumn på `series` räcker — ingen kopplingstabell — och väljaren erbjuder de två förvalen plus varje tagg som redan förekommer bland användarens serier, plus "ny tagg…". Kvar att lösa vid implementation: migrering i `Db.kt`s idempotenta stil, taggen måste rida med i `GET /series?since=` och SQLDelight-cachen annars syns den inte offline, filtret i Historik som chips vid sidan av kaliberchipsen, och om `SeriesExport.kt`/Statistik ska känna till taggen. **Skapandevägen landade 2026-09-16**: `tag` på båda DTO:erna (defaultat), `SeriesRecorder` äger taggen som StateFlow och läser den vid *spar*-tillfället så en omtaggning av en redan fryst bild räknas, senaste taggen sparas i `BackendTokenStore` — samma mekanism som kalibern och Historik-filtren, ingen andra lagring — chip bredvid kaliberchipet i `TotalBadge`, och en egen `TagDialog` med förvalen, varje tagg som redan förekommer bland serierna, och ett fritextfält kapat till 32 tecken (längre är 400 från servern). SQLDelight-cachen behövde ingen schemaändring: hela DTO:n ligger som JSON i `json`-kolumnen, så fältet rider med — de sidokolumner som finns är bara till för sortering. Ett rotorsaksfix utanför bokstaven i uppdraget, av samma sort som admin-editorns: detaljsidans Spara PUT:ar hela serien, så utan `tag` i den bodyn hade varje hål- eller kaliberändring rensat taggen. Kvar att kolla på telefonen: tre chips i samma rad kan bli trångt på en smal skärm. **Visnings- och filtervägen landade samma kväll**: "Tagg:"-rad på detaljsidan med samma lokala state-mönster som kalibern (inte via `SeriesRecorder`, som äger den pågående skanningen), taggen med i Spara-knappens enabled-villkor, multi-select chipsrad i Historik mellan kaliber och datum — raden utelämnas helt när ingen serie har en tagg — och `tag` sist i `series.csv`. Den kluriga biten var persistensen: filtret sparas som `key=value;…` och en fritextagg kan innehålla `;`, `,` och `=`, så taggar procent-escapas med `%` först ut och sist in; utan det hade en tagg med semikolon inte bara tappat sig själv utan rivit hela det sparade filtret vid nästa avkodning. Gamla filtersträngar avkodas fortfarande (fält slås upp per nyckel med default), pinnat av ett test på en literal sträng från förra formatet. Medvetet utelämnat: filter på *otaggade* serier, som hade krävt ett sentinelvärde i chipslistan, predikatet och kodningen; och taggen på historikkortet, som inte efterfrågades. 206 tester gröna |
| 105 | Fler kalibrar i kaliberlistan — "Ta alla vanliga kalibrar" (user 2026-09-16) | done 2026-09-16 — 24 labels (var 9): kantantända `22lr 22wmr 17hmr`, pistol/revolver `32 380 9mm 38 357 40 10mm 44 45`, gevär `223 243 6.5x55 6.5cm 270 308 30-06 7.62x39 8x57 9.3x62 300wm`, `-` kvar först. Samma lista på båda sidor: appens `Caliber` och serverns `CALIBERS`, som annars ger 400 på spara. Inget befintligt label är omdöpt — de ligger i produktionsdatabasen — bara ordningen är ändrad (45/44 efter 40/10mm), och ordningen är ren UI. Konstantnamn: efterföljande bokstäver flyttas först (`22lr`→`LR22`), rena siffror får `C` (`32`→`C32`). Dubbleringen mellan projekten är gjord synlig åt båda håll: varje fil pekar ut den andras fulla sökväg, och `CaliberTest.labelsAreTheAgreedWireValues` + `ApiTest.everyKnownCaliberIsAccepted` stavar ut samma lista (den senare postar en serie per kaliber och läser tillbaka dem). `CaliberDialog` skrollar redan, admin-editorns `caliberSelect()` och Historik-filtret läser listorna, så de följde med gratis. 192 apptester + 59 servertester gröna. **Live sedan omdeployen 2026-09-16 22:07** (användarens körning; fällan var att icke-interaktiv ssh saknar `docker` i PATH — `export PATH=/opt/homebrew/bin:$PATH` före). Verifierat på den körande servern: adminsidans kaliberlistor innehåller `300wm`, `6.5x55` och `9.3x62` |
| 106 | Träffprickarna: skala dem efter kaliber och rita dem genomskinliga, både på fotot och i Statistikens tavelvy (user 2026-09-16: "När vi ritar prickar på tavlan, skala dom efter Kaliber" + "Nuvarande prickar är bra storlek för Kaliber 32" + "På fotot där vi markerar träffar samt tavlan i statistiken vyn, Rita hålen lite 70% transparent") | done 2026-09-16 — `Caliber` fick `diameterMm` (nominell kuldiameter) och en delad `hitDotRadiusMm()`: `HIT_DOT_RADIUS_32_MM * (diameterMm / C32.diameterMm)`. Ordningen på operationerna är inte kosmetisk — agentens första version `2.5f * d / C32.d` gav 2.4999998f för kaliber 32, alltså inte dagens storlek; ratiot först gör `x/x` exakt 1.0f, och testet asserterar utan tolerans så en framtida omskrivning faller igen. `NONE` får 32:ans diameter, vilket håller båda ritställena till ett enda uttryck utan null-gren. Statistiken: `scale` är redan px/mm, kalibern slås upp en gång per serie utanför hålloopen. Fotot: `DetectionOverlay`s `scale` är bild→canvas, inte px/mm, så millimetrarna kommer ur den inpassade ellipsen (`ring.semiMajor / TARGET_BLACK_RING_RADIUS_MM`); utan ring gäller dagens fasta prick, och ett golv på 2 px hindrar att en liten kaliber försvinner. Kalibern plumbas via `LocalSeriesRecorder` inne i `TargetScanner` (localen deklareras i samma fil, så inget nytt beroende och tävlingswizarden får det gratis) och via detaljsidans redigerbara `caliber`-state, så en rättad kaliber ritar om direkt. Alpha 0.30 på träffprickarna i båda vyerna, inklusive statistikens svarta kontur — en opak kontur runt en genomskinlig fyllning tar bort hela poängen när prickar överlappar. **Kalibreringspunkten håller bara i statistiken**: där var prickarna redan 2,5 mm så 32:an är oförändrad, medan fotot inte hade någon mm-storlek att bevara och dess 32:a nu ritas ~2× större än den fasta pixelpricken. Avvaktar användarens omdöme på telefonen. 195 tester gröna |
| 107 | Markera-skärmen: kunna välja kaliber och tagg **medan detekteringen kör**, plus en toggle som slår av/på detektering (user 2026-09-16) | done 2026-09-17 — (a) chipsen var aldrig spärrade: de renderades bara i resultatläget (`TotalBadge`), så `ScanChips()` ritas nu även i `BottomArea`:s live- och processing-gren; ofarligt då `SeriesRecorder.startSave` stämplar kaliber+tagg vid **spartillfället** och dialogerna bor i `AppNavHost`. `AtomicBoolean`-vakten rör bara `startScan`. (b) det finns ingen automatik att stänga av — `startScan` körs bara på knapptryck — så toggeln ("Detektera hål", user-vald 2026-09-17) hoppar över **enbart ONNX-passet**: geometrin körs, tomma hål publiceras så fasen går till IDLE och det frysta fotot blir redigerbart — hålen prickas in för hand mot ringen som just fittades. Session-only state. Gate grön, installerad på telefonen; telefonkoll av själva flödet kvarstår |
| 108 | Markera-skärmen, på den detekterade bilden: rita även ringarna innanför 6/7-ringen och sätt ut 8 och 9 (user 2026-09-16: "Lägg till 8, 9 i markera detekterar bilden samt de ringarna innanför 6-7 ringen") | done 2026-09-16 — 7/8 vid 75 mm, 8/9 vid 50 mm och 9/10 vid 25 mm, plus siffrorna 8 och 9 vid sina mittband (62,5 / 37,5 mm). Härledningen ligger i `vision/ringOutline()`, ren kod bredvid `targetOffsetMm`: eftersom avprojektionen är **affin** kring siffercentrum (rotera, sträck minorkomposanten, skala) blir en cirkel i tavelplanet en ellips med *samma* rotation som 6/7-fittet och halvaxlar `semiMajor*R/100` / `semiMinor*R/100` — centrerad på sifferradernas skärning, aldrig på ellipsens egen mittpunkt. Därmed kan linje och poäng inte säga emot varandra, och testet mäter just det: 16 parametriska punkter på varje ritad oval matas genom `distanceMm` och ska ge tillbaka sin egen radie inom 1e-2 mm, på en roterad (36°) och excentrisk (200×80) ellips med siffercentrum förskjutet — droppa rotationen eller använd ellipsens centrum och testet faller. Ritningen delar nu en privat `drawTargetRing()` med 6/7-ellipsen (identisk rendering som förut), innerringarna på 45 % alpha och 0,6× streck så de läses som hjälplinjer, under hålmarkörerna. **6/7-ellipsen ritas fortfarande på sitt eget fittade centrum** medan innerringarna sitter på siffercentrum: det är avsiktligt, ellipsen är fittet och dess läge mot den svarta kanten är det användaren bedömer — men skiljer sig centrumen syns det som att ringarna inte är koncentriska, vilket i sig är en ärlig signal om att fittet glidit. Ingen 12,5 mm inner-X-cirkel: den hamnar mitt i träffklungan under prickarna (en rads ändring i `INNER_RING_RADII_MM` om den ändå önskas). Avvaktar användarens omdöme på telefonen. 207 tester gröna |
| 109 | Skanningsanimationen: slumpa prickarnas positioner en gång per varv av det gröna svepet (user 2026-09-16: "Medans detektor körs så visar vi en svepande grön effekt samt små gröna prickar, randomisera prickarnas positioner varje varv av den svepande gröna effekten") | done 2026-09-16 — positionerna ligger nu i en ren `scanBlips(random)`: en blip per 72°-hink med 6° marginal och 60° jitter (alltså minst 12° isär, aldrig ovanpå varandra) och radie `0.4f + rnd * 0.5f`, samma spann som de gamla fasta värdena. Varvet räknas av en egen `LaunchedEffect { delay(SCAN_SWEEP_MS); lap++ }` och `remember(lap)` byter listan — `angle` läses fortfarande **bara inne i draw-lambdan**, så Canvas recomposas en gång per varv i stället för per bildruta, vilket är hela poängen när detektorn redan äter CPU:n. Varvtiden är en delad `SCAN_SWEEP_MS = 1300` som både svepets `tween` och räknaren använder, så de inte kan glida isär i koden. Testet kör 50 seeds mot invarianterna (fem blips, vinkel i 0..360, radie i 0,4..0,9, parvis ≥10° isär) i stället för att låsa exakta värden. Två medvetna hörn: `delay` och animationsklockan är skilda timers, så över många varv kan omslumpningen hamna en bildruta vid sidan av sveplinjen (osynligt under en detektering som varar några varv), och radierna är fraktioner av halvaxlarna, så på en kraftigt lutad ellips ligger 0,9 närmare kanten längs minoraxeln — precis som de fasta blipsen gjorde. 208 tester gröna |
| 110 | Dölj debug-ikonen i markera-skärmens topbar i produktionsbyggen (user 2026-09-16: "Ta bort debug meny iconen i markera skärmen för produktion byggen") | done 2026-09-16 — ny `expect val isDebugBuild` (`ui/markera/DebugBuild.kt` + `.android.kt` / `.ios.kt`, samma filnamnsmönster som `DynamicColor`/`CameraPermission`), och `Tune`-knappen i `MarkeraTopBar` ligger nu bakom `if (isDebugBuild)`. Ingen befintlig flagga gick att återanvända: `BuildConfig.BACKEND_URL`/`GOOGLE_CLIENT_ID` är Android-strängar som bara läses i `MainActivity`/`androidMain/series/SignIn.kt`, och `MarkeraDevAuth` är en iOS-plistnyckel för dev-auth — ingen av dem säger något om byggvariant, och ingen är synlig från commonMain. Android-actualen är `BuildConfig.DEBUG`; iOS-actualen är `Platform.isDebugBinary` (`@OptIn(ExperimentalNativeApi::class)`), stdlib i `kotlin.native`, alltså ingen ny plistnyckel och ingen Gradle-plumbing. Allt annat är orört: `showDebug`, `onToggleDebug` och hela `DetectionOverlay`s debug-ritning står kvar, så en debug-build har kvar verktyget. Inget test: ändringen är en plattformskonstant plus ett `if`, och grinden kör debug-varianten så en assertion hade antingen varit fel eller en tautologi — `assembleRelease` (exit 0) är beviset för att release-vägen kompilerar. **Att bekräfta vid nästa iOS-bygge:** `isDebugBinary` speglar framework-länkläget, inte Xcode-konfigurationen direkt — de sammanfaller här (`CONFIGURATION=Release` → `linkReleaseFramework*`), men titta efter ikonen i topbaren nästa gång Release byggs på Mac:en. 208 tester gröna, oförändrad baslinje |
| 111 | Serie-sidan: större text och mer luft på kaliber- och taggraderna (user 2026-09-16: "gör tag och kaliber texterna störra och ge dom liter mer space i serier sidan") | done 2026-09-16 — `titleMedium` → `titleLarge` på båda raderna, både etikett och värde, radavståndet i kolumnen 0 → 6 dp (`Arrangement.spacedBy`) och mellanrummet etikett↔värde 4 → 8 dp. Datumstämpeln står kvar på `titleMedium`, så de två raderna man faktiskt kan trycka på är det ögat fastnar på — hade allt tre vuxit vore hierarkin oförändrad och ändringen meningslös. Ren stiländring utan logik, gjord direkt av orkestratorn i stället för att delegeras: briefen hade blivit större än diffen. 208 tester gröna, oförändrad baslinje |
| 112 | Historik: kom ihåg vilka dagsgrupper som är öppna respektive stängda (user 2026-09-16: "i historik skärmen, kom ihåg vilka datum som är öppna och stängda") | done 2026-09-16 — `encodeOpenDays(days, series, zone)` / `decodeOpenDays(text?)` är rena funktioner i `HistoryFilter.kt`, lagringen en ny nyckel i det befintliga spåret (`BackendSession` med no-op-default, `history_open_days` i DataStore, `markera.backend.historyOpenDays` i NSUserDefaults). Skärmen hissade upp `expanded` bredvid `filter`, tappade `(filter)`-nyckeln och fick `expandedLoaded` + `onToggleDay` som speglar `onFilter`. Alla tre fällorna hanterade: **öppna** dagar lagras, så en ny dag är hopfälld tills den rörs (task 93:s beteende intakt); `expandedLoaded` läses i samma `LaunchedEffect(Unit)` som filtret, så första kompositionens tomma mängd inte kan skriva över; och rensningen bor **inne i** `encodeOpenDays` mot den ofiltrerade listan — ett enda skrivspår, alltså omöjlig att glömma. Separatorn `;` är belagd, inte antagen: dagnyckeln är `localStamp`s datumhalva (siffror och bindestreck, i värsta fall en ISO-stämpels tio första tecken), så ingen escaping behövs till skillnad från fritexttaggarna. Kvarlämnat medvetet: `rememberSaveable` är numera överflödig när butiken är sanningskällan (telefonen är dessutom portrait-låst sedan task 60) — vid processdöd återställs den och skrivs direkt över av det lagrade värdet, alltså ofarlig. 211 tester gröna (+3) |
| 113 | Statistiksidan: filtrera på taggar (user 2026-09-16: "låt statistiksidan filtrera på taggar") | done 2026-09-16 — `StatsFilter` fick `tags: Set<String> = emptySet()` **sist** i konstruktorn, så befintliga positionella anrop står orörda, och predikatet blev en enda rad i `plotSeries` bredvid kaliberguarden: `filter.tags.isNotEmpty() && series.tag !in filter.tags`. `Trend.kt` är därmed orörd och Trend-fliken ärver filtreringen gratis, eftersom den konsumerar de `PlottedSeries` som redan passerat `plotSeries` — det var hela poängen med att kartlägga filterpunkten före briefen. UI:t är en tredje chip-sektion mellan kaliber och datum, flerval som i Historik, med "Alla" som nollställer (återbrukar `stats_caliber_all`, som redan är just "Alla"); enda nya strängen är `stats_group_tag`. Två finesser: `offeredTags = (tags + selectedTags)` gör att en vald tagg vars sista serie försvinner vid en refresh ändå får ett chip, annars hade filtret fastnat i ett läge man inte kunde klicka bort; och hela sektionen uteblir när ingen plottbar serie har en tagg, så skärmen är oförändrad för den som aldrig taggar. Lokalt `tagsWithGeometry()` i stället för Historiks `tagsPresent()` — filen har redan eget `calibersWithGeometry()` bredvid `calibersPresent()`, så det följer konventionen och slipper ett beroende `ui.stats` → `ui.history` för en rad. Filtret sparas **inte**, medvetet: Statistik har ingen filterpersistens till skillnad från Historik. Känt och accepterat: matchningen är skiftlägeskänslig, så "träning" och "Träning" blir skilda chips — samma predikat som Historiks taggfilter, alltså konsekvent mellan skärmarna. 214 tester gröna (+3) |
| 114 | Träffprickarna på tavlan 80 % opaka i stället för 30 % (user 2026-09-16: "markeringarna på tavlan ska inte vara 30% transparenta, utan 80%") | done 2026-09-16 — `HIT_DOT_ALPHA` 0.30f → 0.80f i `series/Caliber.kt`, inget annat. Tolkningen är att siffran byts rakt av: konstanten är alfa, inte transparens, så 80 betyder **mer solida** prickar — hade önskemålet lästs bokstavligt ("80 % genomskinliga") hade värdet blivit 0.20f och prickarna nästan försvunnit, vilket går tvärtemot att 0.30 redan var för blekt (task 106). En enda konstant driver **båda** tavlorna — fototöverlägget (`DetectionOverlay.kt:165,175`) och Statistiks tavla (`StatsScreen.kt:658,665`) — så de följs åt; ska bara den ena ändras måste konstanten delas i två, och det är i så fall en egen rad. Inget test: ett test som spikar 0.80 hade bara upprepat konstanten, och grep visar att inget befintligt test läser den. Kalibreringsknapp satt med ögat på telefonen, precis som `HIT_DOT_RADIUS_32_MM` bredvid. 214 tester gröna, oförändrad baslinje |
| 115 | Historik-listan: visa taggen på varje serie (user 2026-09-16: "i historik listan av serier, visa tag för serier") done 2026-09-16 — en rad till i `SeriesCard`, men taggen ligger **bredvid** kalibern i stället för på egen rad: radhöjden sätts av 72 dp-tumnageln, så en tredje textrad hade gjort varje taggad rad högre än de otaggade och listan ojämn. `bodyMedium`/`onSurfaceVariant` mot kaliberns feta `titleMedium`, alltså syns taggen utan att ta över. Två skydd mot en lång tagg: vänsterkolumnen fick `weight(1f, fill = false)` så den kläms ihop i stället för att knuffa ut totalsumman, och taggtexten `maxLines = 1` + ellips — `normalizeTag` kapar visserligen vid `MAX_TAG_LENGTH`, men den gränsen är serverns, inte layoutens. `takeIf { it.isNotBlank() }` för att en tom tagg annars hade lämnat ett 8 dp-glapp efter kalibern på rader sparade före normaliseringen. Ingen ny sträng och ingen etikett: taggen står som den är, precis som kalibern. Verifierat på telefonen — serie 18:47 visar "22lr träning", otaggade rader oförändrade och lika höga. 214 tester gröna, oförändrad baslinje |
| 116 | Live-sökaren: rita ringlinjerna inne i svarta plus siffrorna 8 och 9 (user 2026-09-16: "cant see the 8 and 9 ring in the markera screen") | done 2026-09-16 — diagnosen först: det **frysta** fotot ritade dem redan sedan task 108, vilket användarens egen skärmdump av Serie-sidan bevisade (samma `DetectionOverlay`, samma vakt `ring != null && centre != null && method != NONE`, och `onGeometryReady` sätter båda så de lever kvar när fasen går till IDLE). Luckan satt alltså i `ViewfinderGuide` — live-överlägget före bilden — som bara ritade 6/7-cirkeln med sina två siffror. Tillagt: tre cirklar på 0.75/0.5/0.25 av guideradien (7/8, 8/9, 9/10-linjerna; radien är per definition 6/7-kanten = 100 mm) med `alpha 0.45` och 0.6× strecktjocklek, samma dämpning som de inre ringarna på fotot — 6/7-cirkeln är den man ramar in mot, resten säger bara var skottet hamnade. Siffrorna blev två nya poster i den befintliga listan, "9" på 0.375 och "8" på 0.625, alltså mittbandet av sin egen ring precis som 6 och 7 redan låg. Medvetet cirklar och inte ellipser: guiden är ett statiskt inramningsstöd, det finns ingen fit före bilden — perspektivet kommer först när ringen är anpassad på den frysta rutan. Inget test: ren ritning utan logik, i en privat composable, och grinden bygger den. 214 tester gröna, oförändrad baslinje |
| 117 | Historik-listan: etiketter på kaliber och tagg, bara klockslag (dagen står i gruppRubriken), "Serie N", och summan ner i raden (user 2026-09-17, layoutval 5 av tio förslag) | done 2026-09-17 — `SeriesCard` är nu sex etiketterade celler på tre delade kolumnvikter (`COLUMN_1/2/3`, den tredje bredast för träfflistan) med hårfina `|` mellan kolumnerna och centrerad text: TID · SERIE · TRÄFFAR / KALIBER · TAGG · SUMMA. Nya `localTime` (Dates.kt) för klockslaget och `dayOrdinals` (HistoryFilter.kt) för serienumret — numrerat mot **ofiltrerade** listan så ett filter inte döper om "serie 3"; båda JVM-testade. Etiketterna återanvänder befintliga strängar, bara `history_time_label` är ny. Gate grön, installerad på telefonen |
| 119 | Sign in with Apple på **bägge** plattformarna (card 55, user: "i meen apple signing for both platforms") | done 2026-09-21 — Android saknade Apple helt och iOS saknade Google, så samma person fick två tomma konton (`Db.upsertUser` nycklar på `(provider, subject)`). Apple har ingen Android-SDK, och webbflödets `client_id` måste vara ett **Services ID** på en Apple-verifierad https-domän med en `client_secret` som är en ES256-JWT — den får aldrig ligga i en APK. Därför medlar backend: appen öppnar webbläsaren mot `GET /auth/apple/start?state=`, servern 302:ar till Apple, `GET /auth/apple/callback` växlar koden (`AppleWeb.kt`, `java.net.http` + java-jwt, inga nya beroenden), verifierar id_token, skapar sessionen och parkerar den i 60 s, 302:ar till djuplänken `markera://auth/apple?state=`, och appen hämtar ut den med `POST /auth/apple/claim {state, secret}`. Säkerheten i djuplänken är att `state` bara är hex-sha256 av en hemlighet som aldrig lämnar telefonen förrän claimet — vilken app som helst kan registrera `markera://`, men bara den som startade flödet kan lösa in. Engångs, och fel hemlighet raderar *inte* posten (annars kunde en gissning bränna någon annans inloggning). `TokenVerifier` tar nu en lista av `aud` och verifierar med `withAnyOfAudience` (`withAudience(vararg)` kräver *alla*), så både iOS bundle-id och Services ID passerar. Google är kvar på Android — varenda befintligt konto är Google-nycklat — så Android fick en väljardialog, iOS är oförändrat. **Ingen kontolänkning**: ett Google-nycklat konto som loggar in med Apple blir ett nytt, separat konto. Kräver fem nya env-vars i Macens `server/.env` (`APPLE_SERVICES_ID`, `APPLE_TEAM_ID`, `APPLE_KEY_ID`, `APPLE_PRIVATE_KEY`, `PUBLIC_URL`) plus portalstegen nedan; utan dem svarar rutterna 503 och allt annat fungerar som förut. 266 apptester + serverns svit gröna |
| 118 | Card 19 code review: follow-ups 19a–19j (save-pipeline races, backup, server hardening, lifecycle, caliber-sized manual scoring, vision/eval dedupe, file splits, i18n, dead code, competition wizard) — see `docs/code-review-card-19.md` | queued 2026-09-19 |
| 119 | Dölj 6/7-ringen och centrumkorset för användare (user 2026-09-21, card 57: "in the markera screen in the photo where we detect holes, stop drawing the center and 6-7 green ring in production, keep them for developers only since they are debug information and not really useful for users") | done 2026-09-21 — båda ritningarna flyttade bakom den **befintliga** `showDebug`-flaggan i `DetectionOverlay` i stället för en ny flagga eller `isDebugBuild`: `if (ring != null)` → `if (showDebug && ring != null)`, och centrumblocket fick `showDebug &&` i sin yttre vakt — då blev den inre `if (showDebug)` runt sifferradslinjerna överflödig och kunde tas bort. Task 110 dölde bara *knappen* i release; det här avslutar jobbet, och menyvalet **Visa detektionsdata** i en debug-build tar tillbaka både ringen och korset. `isDebugBuild` dugde inte som grind på anropsstället: Serie-sidan skickar `detections = emptyList()`, så `showDebug = isDebugBuild` hade tagit debuggrenen som ritar boxar och **tappat traffprickarna**. De inre ringlinjerna (7/8, 8/9, 9/10) står kvar medvetet — task 108 och 116 bad uttryckligen om dem och de markerar gränser som är osynliga på svart. Grinden sitter **inne i** `DetectionOverlay`, alltså förlorar Serie-sidan och tävlingsguiden ringen och korset också: samma bild, samma skäl, en enda vakt där alla anropare passerar. `dotRadius` läser fortfarande `ring.semiMajor`, alltså *värdet* och inte ritningen — prickstorleken är oförändrad. Hjälptexten `help_scan_debug_body` utökad i båda språken. Inget test: en ritflagga i en composable ger antingen en tautologi eller fel assertion; bygget är grinden (samma resonemang som 110 och 116). 263 tester gröna, oförändrad baslinje. Ingen telefon ansluten vid bygget — installationen återstår |
| 9 | HTTPS for the backend (queued 2026-09-06 as "if the backend ever leaves the LAN") | done 2026-09-07 — `https://markera.duckdns.org` via the Mac mini's host Caddy (block appended over ssh, backup `Caddyfile.bak-20260907`); container bound to 127.0.0.1:8090; app default URL switched, cleartext config removed |
| 120 | Kontinuerlig skanning fires nothing on the phone (user 2026-09-23) — debug-build overlay on the viewport (per-tick counters, why the last sample did or did not fire, changed blobs drawn green/red/grey); then per the researched plan: lock AE/AWB + hold centre focus while watching, align ±4 px of camera shake by a SAD block search before differencing, and rebase the reference on every still clean sample so light drift / creep never accumulate | done 2026-09-23 — `ContinuousScanTest` 12/12 (3 new: 3 px shake alone silent, hole fires across a 3 px shake, slow light ramp silent then a hole fires); on the phone; **awaiting the user's verdict from a real target** (the overlay says why a sample is rejected; next step if still flaky: dump vetoed samples as PGM for offline tuning) |
| 121 | Kalibrar som användaren själv lägger till och tar bort i kaliberdialogen, t.ex. `38wc` för 38 wadcutter (user 2026-09-23). Ny kaliber = namn + diameter i mm (diametern styr prickstorlek och kantmätning av handplacerade hål); bara egna kalibrar kan tas bort, de inbyggda är fasta | done 2026-09-23 — `Caliber` is a label-identified class now (built-ins as companion vals, `BUILT_IN` replaces `entries`); `fromLabel(label, custom)` keeps an unknown label with NONE's diameter instead of collapsing to NONE, so `38wc` shows everywhere; the list lives in `SeriesRecorder.customCalibers`, persisted per device through the `BackendTokenStore` seam (DataStore `custom_calibers` / NSUserDefaults), survives sign-out, never synced; the same `CaliberDialog` on the scan screen and the Serie page (there, adding sets that series' caliber). Gate 292 green, iOS framework links; **awaiting the user's phone check** |
| 122 | Continuous scan round 2, step 0 (user 2026-09-23, plan: research round 2): debug builds record the watch frames (ref/prev/cur as PGM in `filesDir/watch/`, last 30 sets, on veto / fire / a moving streak ≥ 5) + `ContinuousScanReplayTest` replaying a pulled folder (`-Dwatch.frames=`) — the instrument steps 123–126 are tuned against | done 2026-09-23 — `WatchVerdict` carries the compared frames + an outcome; the watch loop records FIRE/VETO/every 5th moving tick through `FrameSource.recordWatch` (Android: `filesDir/watch`, 30 sets kept; iOS no-op); pull with `adb exec-out run-as se.kjellstrand.markera tar c files/watch > watch.tar` (Git Bash; verified on the phone); replay `--tests "*ContinuousScanReplayTest" -Dwatch.frames=<dir>`. Gate 295 green, iOS links; **awaiting a recorded tripod session from the user** |
| 123 | Round 2 step 1: sub-pixel-tolerant compare — changed only when outside the 3×3 ref neighbourhood's [min, max] interval (a half-pixel-shifted sharp edge must not fire). **Widened after the first real session (2026-09-23, 52 sets):** nearly every veto was 1 px straight lines along ring/disk/paper edges (sub-pixel residue), and a pinned camera sat in `moving` on 38 noise px vs `SETTLED_MAX_PIXELS` = 6 at the real 600² grid → "settled" becomes "no blob ≥ HOLE_MIN_AREA changed since the previous sample" instead of a pixel count | done 2026-09-23 — interval compare (whole 3×3 inside the frame only) + blob-based settled (`SETTLED_MAX_PIXELS` gone); `NewHoleWatch.seed` makes the replay redo each recorded comparison exactly (30/30 reproduced on the old code). Replay of the 30 kept real sets: before 1 FIRE / 27 VETO / 2 moving / 0 clean → after 6 / 10 / 0 / 14; the 5 new FIREs sit on one spot (sensor 494,481, luma 171→120) that looks like a hole the live watch missed; remaining vetoes = sets 46-51 (light change → 124) and 29/33-35 (a real change near the top edge of the sensor frame). Gate 297 green, iOS links. Follow-ups noted: `HOLE_MIN_FILL` 0.45 may reject torn holes (a 37-40 % blob at 37,259); the grid is really 600², not `WATCH_GRID` 480 |
| 124 | Round 2 step 2: local-light robustness — compare high-passed frames (luma − 15×15 box mean, integral image) instead of a single global offset, plus a gain + offset fit (cur ≈ a·ref + b) so an exposure/cloud change keeps the reference valid; a half-frame soft shadow must not fire, a hole inside it must, and a hole across a 30 % gain change must. The real session showed a light change where the bright wall/paper moved everywhere while the black barely did (sets 51-52) | done 2026-09-23 — least-squares gain+offset (`fitLight`, unclipped pixels, refitted after alignment) + local light = the residual capped at ±`LIGHT_MAX_STEP` 40, clipped left out, box-averaged over `LIGHT_WINDOW` 15 (integral images); a plain per-frame high-pass spread a hole into its window and failed. Replay of the 30 real sets: 6 FIRE / 10 VETO / 14 clean → 0 FIRE / 3 VETO / 7 global→new ref / 20 clean. **Correction to 123:** none of those FIREs were holes — chrome weight plates on the table (494,481 / 506,454) and the wall moulding (37-42,259); 33-35 still veto on a light rim at the disk top edge. Synthetic holes pasted into real frames still fire (r 3-5 bright); a faint dark hole (~23 luma on the black) loses its margin. ~25 ms/offer on desktop JVM (2× before). Gate 301 green, iOS links. Follow-ups: watch only the target's bounding box (all remaining false blobs are off-target); a VETO never moves the reference, so a persistent non-hole change blocks the watch |
| 125 | Round 2 step 3: tile-wise alignment — 6×6 tiles, each its own shift ±2 px around the global one (textured tiles only), covering small rotation / keystone / paper flex; 0.5° rotation alone silent, hole across it fires | queued |
| 126 | Round 2 step 4: temporal averaging — reference = mean of the last 3 clean still samples, analyzer keeps a running mean of the frames it drops between samples (~√3 less noise, room to lower `CHANGE_MIN_DELTA` later) | queued |
| 127 | Kontinuerlig skanning: re-meter the light every ~30 s (user 2026-09-23: "clouds can play nasty tricks") — when idle and settled, unlock AE/AWB, let it converge ~1.5 s, relock; the watch pauses meanwhile (overlay: "calibrating") and keeps its reference (124's gain fit bridges the exposure step), so a shot fired during a re-meter is still found | done 2026-09-23 — `FrameSource.remeter()` (suspend: unlock AE/AWB, `LOCK_AFTER_MS`, relock; focus untouched), Android reaches the bound camera through a new `CameraPreview(onCamera)`; the watch loop re-meters when `remeterDue` (30 s since the last one, last verdict not MOVING/FIRE, no scan busy), drops the frame taken meanwhile and keeps its reference; debug overlay `calibrating…` + `meter N`. Gate 304 green, iOS links, replay unchanged. **Awaiting the phone:** the real recorded exposure steps (×1.15+23) still came out as a global change, so if a re-meter does the same, a shot fired during it becomes part of the new reference and is missed — fix then: scan when the first verdict after a re-meter is a global change |
| 128 | Kontinuerlig skanning help (user 2026-09-23: explain how auto mode works, in a dialog behind a ? like the rest of the app) | done 2026-09-23 — (?) `IconButton` (the menu's `HelpOutline`) beside the switch opens a `HelpDialog` with How it works / Setting up / Good to know, en + sv; the menu help keeps the "how it works" section. Awaiting the phone for the look |
| 129 | Hide the Kontinuerlig skanning switch in production for now (user 2026-09-24), and warn at the next release | done 2026-09-24 — `continuousAvailable` and the menu help section gated on `isDebugBuild` (the switch starts off, so the watch never runs in release); `/release` step 1-pre lists every `isDebugBuild` gate so the warning comes up. Un-gate once 120-127 are proven on a range |

## API (server)

```
GET  /health                       -> 200 {"status":"ok"}
POST /auth/google {idToken}        -> 200 {token, userId}
POST /auth/apple  {idToken}        -> 200 {token, userId}
POST /auth/dev    {subject}        -> 200 {token, userId}   (DEV_AUTH=true only)
POST /series      Bearer, {timestamp (ISO-8601), caliber, holes:[{x?,y?,ring,innerTen,distanceMm?,detectedRing?,detectedInnerTen?,deleted?}]}
                                   (a deleted:true hole is stored for training and never returned)
                                   -> 201 {id}
GET  /series      Bearer, ?limit=N&before=<id>  -> 200 [{id, timestamp, caliber, holes:[...], hasImage, imageWidth, imageHeight}] newest first
DELETE /series/{id}  Bearer          -> 204 (owner only; holes + image removed)
DELETE /account      Bearer          -> 204 (user, sessions, series, holes, images)
POST /series/{id}/image  Bearer, raw image/jpeg body (≤ 5 MB) -> 204
GET  /series/{id}/image  Bearer     -> 200 image/jpeg | 404
POST /series/{id}/image?width=W&height=H   optional: size of the scored frame (markers on the admin photo)
GET  /admin, /admin/users/{id}, /admin/series/{id}[/image]   HTML, HTTP Basic admin:$ADMIN_PASSWORD; off when unset
```

## HTTPS recipe (task 9, apply the day the backend leaves the LAN)
- **Series detail + editing** (requested 2026-09-07, tasks 28a/28b): edits to a saved series stay
  "confirmed vs detected", no flag column — a score edit changes `ring`/`innerTen` and the admin
  keeps showing `8 → 9`; a *moved* marker changes `x`/`y` while the new `detectedX`/`detectedY`
  keep where the detector put it (admin kind gets ", moved"). Moving a marker nulls `distanceMm`
  until tasks 31/32 land (they store the centre + 6/7 ring per series and recompute it). `PUT /series/{id}` (bearer, owner) and `PUT /admin/series/{id}` (Basic) take the
  same body as the POST and replace the series' caliber, timestamp and holes; the admin page edits
  in the browser with a small inline script (no dependencies), added holes there are manual
  (no detected values). UI rule (user, 2026-09-07): measurements and positions (mm, x, y) show
  no decimals, in the app and on the admin pages.


No app code changes: OkHttp (Android) and Darwin (iOS) trust public CAs already.

1. The Mac mini already runs Caddy on the host (`/opt/homebrew/etc/Caddyfile`, root launchd
   service, Let's Encrypt for thinnikatech.se; 80/443 are already forwarded from the router, WAN
   IP 92.34.29.118 on 2026-09-07). So: no Caddy container. Add a DNS A record for the chosen
   hostname (chosen: `markera.duckdns.org`, DuckDNS) → the WAN IP, then a Caddyfile block
   `<host> { reverse_proxy 127.0.0.1:8090 }` and `sudo caddy reload --config /opt/homebrew/etc/Caddyfile`.
2. `server/docker-compose.yml`: publish `127.0.0.1:8090:8080` so plain HTTP is only reachable
   through Caddy on the same box.
3. App: `markera.backend.url=https://<host>` in `gradle.properties`; drop the cleartext
   entry for 192.168.1.191 from `network_security_config.xml`.
4. `DEV_AUTH=false` and a strong `ADMIN_PASSWORD` in `server/.env` (done in task 24) — `/auth/dev`
   is a free login and `/admin` shows every user's series.

## Follow-ups / out of scope

- Legacy series (saved before the upload carried the frame size) show no markers on the admin
  photo. Frame sizes are known: camera flavor on the OnePlus = 1440×1440 (`previewView.bitmap`
  of the square viewport, confirmed in logcat 2026-09-07), mock flavor = 1203×1203 (dataset
  images downscaled to 1600 on the long side, then centre-squared; two 3072×4096 images give
  1200). One-off SQL on the Mac mini (blocked for the orchestrator, user to run):
  `docker run --rm -v server_markera-data:/data alpine sh -c "apk add -q sqlite && sqlite3 /data/markera.db \"UPDATE series SET image_width=1440, image_height=1440 WHERE image_width IS NULL AND user_id=7; UPDATE series SET image_width=1203, image_height=1203 WHERE image_width IS NULL AND user_id IN (3,9);\""`

- Wizard: corrections made in the wizard's own Confirm picker (`wizardVm.updateShot`) are not
  what `resetScanner(save = true)` passes to `recorder.commit` (it passes the scanner's
  `topScores`), so a series saved from the wizard carries the detector values, not the lane's
  edits. Low priority while the competition entry is hidden (task 20); fix = commit with the
  lane's shots at lane confirmation.

- ~~Image upload alongside a series~~ → task 8.
- ~~Series history screen in the app~~ → task 7.
- ~~HTTPS / exposing the backend outside the LAN~~ → task 9.
